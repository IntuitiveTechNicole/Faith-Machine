import React, { useState, useEffect } from 'react';
import { 
  Layout, Target, Zap, MessageSquare, TrendingDown, 
  Search, Shield, Users, ArrowRight, Camera, Video, 
  FileText, CheckCircle, RefreshCw, Smartphone
} from 'lucide-react';

const FaithMachineMaster = () => {
  const [view, setView] = useState('dashboard');
  const [selectedLead, setSelectedLead] = useState(null);
  const [activeDetailTab, setActiveDetailTab] = useState('verdict');

  // 1. SMART FINDER ENGINE (Zero-Typing)
  const smartNiches = [
    { label: "Faith Ecom (No Pixel)", q: "Christian+Store+Shipping+-pixel" },
    { label: "Church Merch (DM Only)", q: "Church+Tshirt+DM+to+order" },
    { label: "Homeschool (Old Design)", q: "Homeschool+Planners+Powered+by+Shopify" },
    { label: "Local Service (No Web)", q: "Plumber+OR+Landscaper+-site:.com" },
    { label: "Modest Fashion (Slow)", q: "Modest+Clothing+Sale" }
  ];

  // 2. DAILY ATTACK PLAN (Automated Priority)
  const attackPlan = [
    { name: "Grace Planners", task: "Follow up (Touch 2)", reason: "Sent video 2 days ago" },
    { name: "Kingdom Tees", task: "Reply Needed", reason: "Skeptical Vibe detected" }
  ];

  return (
    <div className="flex flex-col h-screen bg-slate-50 text-slate-900 max-w-md mx-auto shadow-2xl border-x">
      
      {/* --- TOP STATUS BAR --- */}
      <header className="p-4 bg-white border-b flex justify-between items-center sticky top-0 z-50">
        <div className="flex items-center gap-2">
          <div className="bg-indigo-600 p-1.5 rounded-lg text-white">
            <Zap size={18} fill="currentColor" />
          </div>
          <h1 className="font-black text-lg tracking-tight italic">FAITH MACHINE</h1>
        </div>
        <div className="flex items-center gap-2">
           <span className="text-[10px] font-bold bg-green-100 text-green-700 px-2 py-1 rounded-full uppercase tracking-tighter">Machine Live</span>
           <Shield size={18} className="text-slate-300" />
        </div>
      </header>

      <main className="flex-1 overflow-y-auto pb-24 p-4">
        
        {view === 'dashboard' && (
          <div className="animate-in fade-in slide-in-from-bottom-4 duration-500">
            {/* PIPELINE STATS */}
            <div className="grid grid-cols-3 gap-2 mb-6">
              <div className="bg-white p-3 rounded-2xl border text-center">
                <p className="text-[10px] font-bold text-slate-400 uppercase">Leads</p>
                <p className="text-xl font-black">124</p>
              </div>
              <div className="bg-white p-3 rounded-2xl border text-center">
                <p className="text-[10px] font-bold text-slate-400 uppercase">Hot</p>
                <p className="text-xl font-black text-orange-500">12</p>
              </div>
              <div className="bg-white p-3 rounded-2xl border text-center border-green-200 bg-green-50">
                <p className="text-[10px] font-bold text-green-600 uppercase">Won</p>
                <p className="text-xl font-black text-green-700">$0k</p>
              </div>
            </div>

            {/* ATTACK PLAN */}
            <h2 className="text-xs font-bold text-slate-400 uppercase tracking-widest mb-3 flex items-center gap-2">
              <Target size={14} /> Daily Attack Plan
            </h2>
            <div className="space-y-2 mb-6">
              {attackPlan.map(item => (
                <div key={item.name} className="bg-indigo-900 text-white p-4 rounded-2xl shadow-lg relative overflow-hidden active:scale-95 transition-transform">
                  <p className="text-[10px] font-bold uppercase opacity-60">{item.task}</p>
                  <p className="font-bold text-lg">{item.name}</p>
                  <p className="text-xs opacity-80 italic">{item.reason}</p>
                  <div className="absolute right-4 bottom-4"><ArrowRight size={20} /></div>
                </div>
              ))}
            </div>

            {/* SMART FINDER (ZERO TYPING) */}
            <div className="flex justify-between items-center mb-3">
              <h2 className="text-xs font-bold text-slate-400 uppercase tracking-widest">Smart Finder</h2>
              <button className="text-[10px] font-bold bg-slate-200 px-2 py-1 rounded uppercase">Shuffle</button>
            </div>
            <div className="grid grid-cols-2 gap-2 mb-6">
              {smartNiches.map(n => (
                <button key={n.label} className="bg-white border p-3 rounded-xl text-left text-xs font-bold shadow-sm flex items-center justify-between group active:bg-indigo-50"
                  onClick={() => window.open(`https://www.facebook.com/ads/library/?q=${n.q}&ad_type=all`)}>
                  {n.label} <ArrowRight size={12} className="opacity-0 group-hover:opacity-100" />
                </button>
              ))}
            </div>
          </div>
        )}

        {view === 'analyze' && (
          <div className="animate-in fade-in duration-500">
             <h2 className="text-xl font-black italic mb-4">Deep Analyze</h2>
             <div className="space-y-4">
                <div className="bg-white p-4 rounded-2xl border">
                  <label className="text-[10px] font-bold uppercase text-slate-400">Website or FB URL</label>
                  <input type="text" placeholder="https://..." className="w-full border-0 bg-slate-50 rounded-lg mt-1 p-3 text-sm focus:ring-2 ring-indigo-500" />
                  <button className="w-full bg-indigo-600 text-white font-bold py-3 rounded-xl mt-3 shadow-lg shadow-indigo-200 flex items-center justify-center gap-2">
                    <Zap size={18} fill="currentColor" /> RUN FULL AUDIT
                  </button>
                </div>
                <p className="text-[10px] text-center text-slate-400 px-6">
                  Machine will check Pixel, Speed, Design, Copy, and Facebook Ad Library automatically.
                </p>
             </div>
          </div>
        )}

        {view === 'intel' && (
          <div className="animate-in fade-in duration-500">
             <h2 className="text-xl font-black italic mb-4">Reply Handler</h2>
             <div className="bg-white p-4 rounded-2xl border border-dashed border-indigo-300">
                <label className="text-[10px] font-bold uppercase text-indigo-600">Paste Full Thread</label>
                <textarea rows="8" className="w-full bg-slate-50 border-0 mt-2 rounded-xl text-xs italic p-3" placeholder="Paste the Facebook DM history here..."></textarea>
                <button className="w-full bg-indigo-600 text-white font-bold py-4 rounded-xl mt-2 flex items-center justify-center gap-2">
                  <MessageSquare size={18} /> OUTSMART REPLY
                </button>
             </div>
          </div>
        )}

      </main>

      {/* --- NAVIGATION BAR (THUMB REACH) --- */}
      <nav className="fixed bottom-0 w-full max-w-md bg-white border-t flex justify-around p-4 pb-8 z-50">
        <button onClick={() => setView('dashboard')} className={`flex flex-col items-center gap-1 ${view === 'dashboard' ? 'text-indigo-600' : 'text-slate-400'}`}>
          <Layout size={20} />
          <span className="text-[9px] font-bold uppercase tracking-tighter">Dashboard</span>
        </button>
        <button onClick={() => setView('analyze')} className={`flex flex-col items-center gap-1 ${view === 'analyze' ? 'text-indigo-600' : 'text-slate-400'}`}>
          <Search size={20} />
          <span className="text-[9px] font-bold uppercase tracking-tighter">Analyze</span>
        </button>
        <button onClick={() => setView('intel')} className={`flex flex-col items-center gap-1 ${view === 'intel' ? 'text-indigo-600' : 'text-slate-400'}`}>
          <MessageSquare size={20} />
          <span className="text-[9px] font-bold uppercase tracking-tighter">Outsmart</span>
        </button>
        <button className="flex flex-col items-center gap-1 text-slate-400">
          <Users size={20} />
          <span className="text-[9px] font-bold uppercase tracking-tighter">Pipeline</span>
        </button>
      </nav>
    </div>
  );
};

export default FaithMachineMaster;